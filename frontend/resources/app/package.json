{
  "name": "paper",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "jfs": "^0.2.6",
    "node-fetch": "~1.3.0",
    "react": "^0.13.3",
    "react-router": "^0.13.3"
  },
  "scripts": {
    "watch": "babel --watch ./source --out-dir ./build",
    "server": "cd ../../../backend && php -S localhost:8000",
    "start": "electron ."
  }
}
